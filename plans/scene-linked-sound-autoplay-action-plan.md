# Plan — Default media play action for scene-linked sound

## Objective

Ensure scene-linked sound renderables generated by the builder are emitted as `VIDEO` persos (for future subtitle handling) and include a default `media` action set to play at sequence start.

## Steps

- [x] 1. Update scene-linked sound renderable generation to include a default `intro` media action payload.
- [x] 2. Add smoke coverage asserting this default action contract.
- [x] 3. Run targeted smoke verification.
- [x] 4. Migrate scene-linked sound perso type from `SOUND` to `VIDEO` and align tests.
- [x] 5. Re-run targeted smoke verification after migration.

## Definition of done

- Builder output for scene-linked sound includes `type: "VIDEO"`.
- Builder output for scene-linked sound includes `actions.intro.media = { action: "play", changeAt: 0, offset: 0 }`.
- Existing non-sound behavior is unchanged.
- Targeted smoke tests pass.

## Review

- Updated `app/player/builder/renderables.ts` so scene-linked sound persos include `actions.intro.media` with `{ action: "play", changeAt: 0, offset: 0 }` by default.
- Updated `app/player/builder/renderables.ts` so scene-linked sound persos are now emitted as `VIDEO` (instead of `SOUND`) to align future subtitle workflows.
- Updated `app/player/builder/renderables.ts` so scene-linked sound persos include `initial.attr.hidden = "hidden"` to avoid visual interference in the scene.
- Updated `tests/scene-linked-sound-builder-smoke.ts` to assert this new media action contract.
- Updated `tests/scene-linked-sound-builder-smoke.ts` expectations to `VIDEO`.
- Verification run: `npx tsx tests/scene-linked-sound-builder-smoke.ts && npx tsx tests/builder-slot-style-smoke.ts` (pass).
