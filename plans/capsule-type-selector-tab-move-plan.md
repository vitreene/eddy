# Capsule Type Selector Tab Move Plan

## Scope

- Dans `item-edit` (vue capsule), deplacer le composant de type capsule (`CapsuleGridTypeSelector`) de l'onglet `Advanced` vers `Presets`.

## Checklist

- [x] Mettre `CapsuleGridTypeSelector` dans `TabsContent value="presets"` de `capsule-edit.tsx`.
- [x] Retirer `CapsuleGridTypeSelector` de l'onglet `Advanced`.
- [ ] Verifier manuellement l'ordre des controles dans l'onglet Presets.

## Verification

- [x] Typecheck.
